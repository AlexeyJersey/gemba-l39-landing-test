{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://AlexeyJersey.github.io/gemba-l39-landing-test",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -R src/* dist/",
    "deploy": "gh-pages -d dist"
  }
}
